
int numofsubset(int arr[], int n)
    {
        // Your code goes here
        
        
        sort(arr,arr+n);
         int count=1;
         for(int i=1;i<n;i++){
             if(arr[i]!=arr[i-1]+1){
                 count++;
             }
              
         }
         return count;
    }
